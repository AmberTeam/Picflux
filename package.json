{
  "name": "amber-cinema",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm i --prefix client && npm run build --prefix client && npm i --prefix server && npm run prod --prefix server",
    "dev": "concurrently \"npm run start --prefix client\" \"npm run dev --prefix server\"",
    "prod": "npm run prod --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.0",
    "concurrently": "^7.5.0"
  }
}
